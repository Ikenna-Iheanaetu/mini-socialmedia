{
  "name": "server",
  "version": "1.0.0",
  "description": "Backend server for the mini social media clone",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "nodemon src/server.js",
    "dev": "pm2 start src/server.js -i -max -f --watch",
    "stop": "pm2 stop all",
    "monit": "pm2 monit --watch",
    "restart": "pm2 restart all"
  },
  "author": "Ikenna Iheanaetu",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^7.3.1",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.22"
  }
}
